# runs 协作规范

## 1. 目录职责

- 本目录存放训练日志, checkpoint, 生成候选原始输出和其他较重运行产物.
- 运行产物按阶段目录归档, 例如 `phase4_rule_repair/`, `phase5_ranker/`.

## 2. 产物规则

- 默认不提交 checkpoint, 大型日志, 大型 SDF 或批量候选文件.
- 需要保留可复现实验信息时, 优先把轻量 summary 写入 `reports/`.
- 运行目录可以包含机器相关临时文件, 但不应成为源码或配置依赖.
- 清理运行产物前先确认, 避免删除用户仍需要的结果.
